/**
 * Infrastructure facade for GPU shape rendering.
 * All consumers should import GPU shape utilities from here instead of @/lib/gpu-shapes.
 */

export { ShapeRenderPipeline } from '@/lib/gpu-shapes'
export type { GpuShapeRect, GpuShapeRenderParams } from '@/lib/gpu-shapes'
